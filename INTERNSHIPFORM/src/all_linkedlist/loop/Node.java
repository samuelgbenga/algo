package all_linkedlist.loop;

public  class Node {
    int data;      // Value stored in the node
    Node next;     // Pointer to the next node

    // Constructor
    Node(int data) {
        this.data = data;
        this.next = null;
    }

}